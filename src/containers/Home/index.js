import React, { Component } from 'react';

import Map from '../../components/Map';

class Home extends Component {
  state = {
  }

  componentWillMount() {
  }

  render() {
    return (
      <div>
        <Map />
      </div>
    );
  }
}

export default Home;
